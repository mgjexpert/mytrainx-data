# WKT Manifest

The current verified WKT workout IDs live in `mytrainx-platform/lib/workouts.ts` until the Supabase migration is complete.

This directory is for content-source metadata and import manifests, not for duplicating canonical runtime IDs indefinitely.

Next:
- export WKT source metadata from the application seed;
- inventory supporting WKT PDFs/manuals;
- add transcript status per video;
- define AI retrieval policy for supporting material.
